'use strict';

angular.module('websiteApp')
    .controller('MainCtrl', function($scope) {
        $scope.news = ['asdf est un titre', 'asdf est un titre', 'asdf est un titre', 'asdf est un titre', 'asdf est un titre', 'asdf est un titre', ];
    });